package com.nutrimind.repository;

import com.nutrimind.entity.RestauranteIfood;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RestauranteIfoodRepository extends JpaRepository<RestauranteIfood, Long> {
}
